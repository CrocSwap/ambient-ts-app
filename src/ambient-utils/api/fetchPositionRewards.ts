import { USE_MOCK_POSITION_REWARDS_DATA } from '../constants';
import { PositionRewardsDataIF } from '../types';

const USE_MOCK_DATA =
    USE_MOCK_POSITION_REWARDS_DATA !== undefined
        ? USE_MOCK_POSITION_REWARDS_DATA
        : true;
