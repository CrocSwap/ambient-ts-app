import { txActionsType } from './TransactionRow2';

interface propsIF {
    width: number;
    actions: txActionsType[];
}

export default function TxButtonBank(props: propsIF) {
    const { width, actions } = props;

    return (<></>);
}